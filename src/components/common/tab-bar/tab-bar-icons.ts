import { ClipboardList, Home, LucideIcon, User, Wrench } from 'lucide-react-native';

export const TabIcons: Record<string, LucideIcon> = {
  home: Home,
  services: Wrench,
  orders: ClipboardList,
  profile: User,
};
